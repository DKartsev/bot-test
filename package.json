{
  "name": "bot-test",
  "version": "1.0.0",
  "description": "Support bot with operator admin panel",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/backend && npm run build -w packages/operator-admin",
    "build:prod": "NODE_ENV=production npm run build -w packages/backend && NODE_ENV=production npm run build -w packages/operator-admin",
    "dev": "npm run dev -w packages/backend",
    "test": "npm run test -w packages/backend",
    "test:coverage": "npm run test:coverage -w packages/backend",
    "lint": "echo 'Linting disabled in root project - use workspace-specific commands'",
    "lint:fix": "npm run lint:fix -w packages/backend",
    "prepare": "node -e \"if (process.env.NODE_ENV !== 'production') { require('child_process').execSync('husky install', {stdio: 'inherit'}) }\"",
    "postinstall": "node -e \"if (process.env.NODE_ENV === 'production') { console.log('Skipping husky install in production'); } else { console.log('Development environment detected'); }\"",
    "pre-commit": "lint-staged"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0"
  },
  "lint-staged": {
    "*.{js,ts,tsx,json,md,yaml,yml}": [
      "echo 'Checking file format...'"
    ]
  }
}
