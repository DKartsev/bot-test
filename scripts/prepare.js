#!/usr/bin/env node

/**
 * Prepare script - temporarily disabled husky installation
 * This script is disabled for deployment fixes
 */

console.log('🚀 Husky installation temporarily disabled for deployment fixes');
console.log('✅ Skipping all git hooks setup');
console.log('💡 This will be re-enabled after deployment issues are resolved');

process.exit(0);
