#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Запуск pre-commit проверок..."

# Проверяем линтинг
echo "📝 Проверка линтинга..."
npm run lint
if [ $? -ne 0 ]; then
  echo "❌ Ошибки линтинга обнаружены. Исправьте их перед коммитом."
  echo "💡 Используйте 'npm run lint:fix' для автоматического исправления."
  exit 1
fi

# Запускаем тесты с покрытием
echo "🧪 Запуск тестов с покрытием..."
npm run test:coverage
if [ $? -ne 0 ]; then
  echo "❌ Тесты не прошли. Исправьте ошибки перед коммитом."
  exit 1
fi

# Проверяем покрытие тестами
echo "📊 Проверка покрытия тестами..."
COVERAGE_FILE="./packages/backend/coverage/coverage-summary.json"

if [ -f "$COVERAGE_FILE" ]; then
  # Извлекаем покрытие из JSON
  STATEMENTS=$(node -e "console.log(require('$COVERAGE_FILE').total.statements.pct)")
  BRANCHES=$(node -e "console.log(require('$COVERAGE_FILE').total.branches.pct)")
  FUNCTIONS=$(node -e "console.log(require('$COVERAGE_FILE').total.functions.pct)")
  LINES=$(node -e "console.log(require('$COVERAGE_FILE').total.lines.pct)")
  
  echo "📈 Текущее покрытие:"
  echo "   Statements: ${STATEMENTS}%"
  echo "   Branches: ${BRANCHES}%"
  echo "   Functions: ${FUNCTIONS}%"
  echo "   Lines: ${LINES}%"
  
  # Проверяем минимальные требования
  STATEMENTS_NUM=$(echo $STATEMENTS | sed 's/%//')
  BRANCHES_NUM=$(echo $BRANCHES | sed 's/%//')
  FUNCTIONS_NUM=$(echo $FUNCTIONS | sed 's/%//')
  LINES_NUM=$(echo $LINES | sed 's/%//')
  
  if (( $(echo "$STATEMENTS_NUM < 80" | bc -l) )) || \
     (( $(echo "$BRANCHES_NUM < 80" | bc -l) )) || \
     (( $(echo "$FUNCTIONS_NUM < 80" | bc -l) )) || \
     (( $(echo "$LINES_NUM < 80" | bc -l) )); then
    echo "⚠️  Покрытие тестами ниже 80%. Рекомендуется улучшить покрытие."
    echo "💡 См. TEST_COVERAGE_PLAN.md для плана улучшения."
  else
    echo "✅ Покрытие тестами соответствует требованиям (≥80%)"
  fi
else
  echo "⚠️  Файл покрытия не найден. Убедитесь, что тесты запущены с --coverage."
fi

# Проверяем форматирование кода
echo "🎨 Проверка форматирования..."
if command -v prettier >/dev/null 2>&1; then
  npx prettier --check "packages/backend/src/**/*.{ts,tsx}"
  if [ $? -ne 0 ]; then
    echo "❌ Код не отформатирован. Используйте 'npx prettier --write' для исправления."
    exit 1
  fi
  echo "✅ Форматирование кода корректно"
else
  echo "ℹ️  Prettier не установлен. Пропускаем проверку форматирования."
fi

# Проверяем типы TypeScript
echo "🔍 Проверка типов TypeScript..."
cd packages/backend
npx tsc --noEmit
if [ $? -ne 0 ]; then
  echo "❌ Ошибки типов TypeScript. Исправьте их перед коммитом."
  exit 1
fi
cd ../..

echo "✅ Все pre-commit проверки пройдены успешно!"
echo "🚀 Можете делать коммит!"
